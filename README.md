# WikipediaSearchTool
A simple Python script that can fetch any topic from Wikipedia using the Pywhatkit library (made by sidharta310312 on github)

Used libraries : Pygame, Pywhatkit

If you haven't installed both libraries, run these 2 commands in CMD (if you have pip) :
```pip install pygame-ce```
```pip install pywhatkit```
